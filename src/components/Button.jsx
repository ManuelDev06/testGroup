import React from 'react'
import '../theme/Button.css'

const Button = (text, onClick) => {
  //Button need to receive the event and the text of the button
  return (
    <div>Button</div>
  )
}

export default Button